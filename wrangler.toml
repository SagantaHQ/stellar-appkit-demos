name = "stellar-appkit-examples"
compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next"

# Cloudflare Pages project — bind via the dashboard or `wrangler pages project create`.
# This config is used by `npm run preview` and `npm run deploy`.
